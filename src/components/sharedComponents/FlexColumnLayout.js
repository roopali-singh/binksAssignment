import React from "react";
import "../../style/sharedComponetsStyle/FlexColumnLayout.css";

function FlexColumnLayout({ children }) {
  return <div className="flexColumnLayout">{children}</div>;
}

export default FlexColumnLayout;
